[package]
name = "content-build"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
pulldown-cmark = "0.13"
termblog-config = { path = "../../config" }
unicode-width = "0.2"
